# Company logo assets

The company logo was supplied by the website owner on 19 September 2026. The supplied screenshot included a gray checkerboard background and large margins.

The selected cleanup was created with the built-in ImageGen tool. This is a cleaned raster rendition of the supplied mark, not an original vector master. Its alpha transparency is preserved. The website uses a small WebP derivative so the full-resolution file does not need to load on every page.

- `assets/sehati-logo-master.png`: 1254 × 1254 transparent PNG.
- `assets/sehati-logo.webp`: 256 × 256 transparent WebP, displayed at 39–52 CSS pixels.
- `assets/sehati-favicon.png`: 96 × 96 transparent PNG.

Sharp was used only to resize and encode the selected generated image for web delivery.

## Final ImageGen prompt

Create a precisely cleaned, digitally redrawn version of THIS exact supplied red logo. Reproduce the central red connected geometric SHT monogram and thin red enclosing circle faithfully, with identical silhouette/proportions/counters. The red areas must be absolutely FLAT uniform solid #c91920, with mathematically smooth clean edges, no texture, no noise, no extra flecks, no speckles, no shadows or gradients. All the gray checkerboard and gray screenshot content must be completely removed. Do NOT simply erase the gray: render the red forms as clean flawless geometric curves and straight lines preserving this exact company logo. Square canvas, centered and neatly cropped with 4% padding. True transparent background. Absolutely no noisy red pixels outside the mark or in the negative spaces. Do not change the shape or redesign the logo, no text additions. Website-ready crisp logo asset.
